<head>
    <!--<link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">-->

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>John Trinh {{ title }}</title>
    <meta name="theme-color" content="{{ color }}">

    <!-- ================== Essential Meta Tags ================== -->
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="description" content="{{ description | default('there is no one like John Trinh!') }}">
    <meta name="keywords" content="John Trinh, portfolio, musician, audio engineer, developer, designer">
    <meta name="author" content="John Trinh">
    <meta name="robots" content="index, follow">

    <!-- ================== Open Graph / Social Sharing ================== -->
    <meta property="og:title" content="John Trinh {{ title }}">
    <meta property="og:description" content="{{ description | default('Personal website of John Trinh') }}">
    <meta property="og:type" content="website">
    <meta property="og:url" content="{{ url | default('https://john-trinh.com') }}">
    <meta property="og:image" content="{{ og_image | default('/assets/og-image.png') }}">

    <!-- ================== Twitter Card ================== -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="John Trinh {{ title }}">
    <meta name="twitter:description" content="{{ description | default('Personal website of John Trinh') }}">
    <meta name="twitter:image" content="{{ og_image | default('/assets/og-image.png') }}">
    <meta name="twitter:site" content="@johntrinh">

    <!-- ================== Favicons ================== -->
    <link rel="icon" type="image/x-icon" href="/assets/favicon/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/favicon/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">

    <!-- ================== Canonical URL ================== -->
    <link rel="canonical" href="{{ url | default('https://john-trinh.com') }}">

    {#
    <!-- ================== Preconnect/Preload ================== -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    #}

    {#
    <link rel="preload" href="/assets/fonts/Karrik-Italic.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    <link rel="preload" href="/assets/fonts/Karrik-Regular.woff2" as="font" type="font/woff2" crossorigin="anonymous">
    #}

    <!-- favicon stuff -->
    {#
    <link rel="icon" type="image/x-icon" href="/assets/favicon/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="/assets/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/assets/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/assets/favicon/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">
    <!-- (android-chrome favicon assignments are in site.webmanifest !) -->
    #}

    {#<link rel="stylesheet" href="/assets/css/main.css">#}
    {#<script type="module" src="/assets/js/main.js"></script>#}
    {# <style>
            :root {
                --page-color: {{color or parentData.data.color}};
            }
        </style> #}

    {# if it isn't local dev server, add gtag #}
    {#
    {% if eleventy.env.runMode != 'serve' %}
        {% include "gtag.njk" %}
    {% endif %}
    #}
</head>