// Minimal TypeScript entry for the museum dev build.
// Keep this tiny to avoid reorganizing the site. Expand in `museum` branch.

import * as THREE from 'three';

// Re-export a noop for now; the real scene code lives in `museum` branch.
export default {};
