// Minimal TypeScript entry for museum. Kept tiny so global tooling can watch/bundle.
import * as THREE from 'three';

// Export nothing; this file can be expanded in the `museum` branch with the full code.
export default {};
